# conda env create -f environment.yml

name: fin-math
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - numpy>=2.0.0
  - pandas>=2.2.0
  - scipy
  - matplotlib
  - jupyterlab
  - pytest
  - pip:
    - yfinance
    - riskfolio-lib
